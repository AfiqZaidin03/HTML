// Old ternary operators
if (true) {

} else {

}

// New ternary operators
// let age = 20;
// let name = age > 10 ? "Afiq" : "Muhd";

// Object
const person = {
    name: "Afiq",
    age: 20,
    isMarried: false,
}

// Old object call
const nama = person.name;
const umur = person.age;
const isKahwin= person.isMarried;

// New object call
const {name, age, isMarried} = person;

